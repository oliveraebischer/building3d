# Generated for `fly launch` / `fly deploy` — adjust app name/region as needed.
app = "building3d-frontend"
primary_region = "fra"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
